package ng.com.caleb.project

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle


/**
 * This is the entry point of our application. What it does is just check if any user is authenticated
 * and then redirects them to either the authentication screen or the main activity.
 */
class EntryActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
    }
}